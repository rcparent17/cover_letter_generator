CoverLetterGenerator
	self.template = HTML data from template squashed and stripped into a one line string
	self.resume_file = the path to the base resume file
	self.companies = the YAML dictionary containing the company entries
	self.output_dir = the directory to output the generated PDF files
	def constructor()
	def generate_letter()
	def _generate_letter_filename()
	def _generate_resume_filename()
	def merge_docs()
	def _populate_template()

helper functions
	def collect_args()
	def read_companies()
	def to_snake_case()
	def is_valid_companies_yaml()